module github.com/dmdhrumilmistry/stunner/core

go 1.24

// NOTE: The skeleton intentionally depends only on the Go standard library so
// it builds offline and in CI without fetching modules. External libraries
// (pion/webrtc, go.mau.fi/libsignal, libp2p, go-sqlcipher, x/mobile) are
// introduced per phase as described in ../docs/ROADMAP.md.
